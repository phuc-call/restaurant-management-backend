package com.example.shop.entity.enums;

public enum TableStatus {
    AVAILABLE,
    RESERVED,
    OCCUPIED,
    ORDERING,
    INACTIVE,    // ẩn bàn (xóa mềm)
    CLEANING,
    DISABLED,
    CLOSED,
}